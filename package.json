{
  "private": true,
  "dependencies": {
    "foundation-sites": "~6.4.3",
    "motion-ui": "^2.0.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "./node_modules/.bin/node-sass-chokidar sass/ -o css/ --watch --recursive --include-path=sass --include-path=node_modules"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^5.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-rename": "^1.4.0",
    "gulp-replace": "^1.0.0",
    "gulp-sass": "^4.0.1",
    "gulp-scss-combine": "^1.0.0"
  }
}
