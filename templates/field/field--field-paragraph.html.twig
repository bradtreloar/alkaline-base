{#
/**
 * @file
 * Theme override for a paragraph field
 *
 * The body is rendered without any wrapper because its content 
 * provide their own wrappers.
 */
#}
{% for item in items %}
  {{ item.content }}
{% endfor %}
