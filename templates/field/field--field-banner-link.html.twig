{#
/**
 * @file
 * Theme override for links in a Hero Banner.
 *
 * Links are rendered as a group of buttons.
 */
#}
<div class="button-group">
  {% for item in items %}
    <a href="{{ item.url }}" class="button">
      <span>{{ item.title }}</span>
    </a>
  {% endfor %}
</div>
