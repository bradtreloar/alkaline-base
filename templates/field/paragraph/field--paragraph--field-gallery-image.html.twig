{#
/**
 * @file
 * Theme override for a cards field.
 *
 * Classes are set in preprocessor.
 */
#}
<ul {{ attributes.addClass('gallery') }}>
  {% for item in items %}
  <li {{ item.attributes }}>
    {{ item.content }}
  </li>
  {% endfor %}
</ul>
