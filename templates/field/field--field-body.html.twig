{#
/**
 * @file
 * Theme override for the `field_body` field which is used in nodes and paragraphs.
 * This is different than the `body` field in that this is a paragraph reference
 * field while the `body` field is just formatted text.
 *
 * The body is rendered without any wrapper because all of its content can be
 * assumed to be block elements.
 */
#}
{% for item in items %}
  {{ item.content }}
{% endfor %}
