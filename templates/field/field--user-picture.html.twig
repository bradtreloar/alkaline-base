{#
/**
 * @file
 * Theme override for a user picture.
 *
 * The user's picture is rendered as a figure without a caption.
 */
#}
<figure class="user-picture">
  {% for item in items %}
    <picture>
      {{ item.content }}
    </picture>
  {% endfor %}
</figure>