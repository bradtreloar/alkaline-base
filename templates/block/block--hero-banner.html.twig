{{ attach_library('alkaline/banner') }}
<header{{ attributes.addClass('block') }}>
  {{ title_prefix }}
  {{ title_suffix }}
  <figure class="banner hero">
    <figcaption>
      {% block content %}
        {{ content }}
      {% endblock %}
    </figcaption>
  </figure>
</header>
{{ css }}
