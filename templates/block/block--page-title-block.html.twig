{#
/**
 * @file
 * Theme override to display a block.
 */
#}
{% if not hidden %}
  {{ attach_library('alkaline/page_title') }}
  <header{{ attributes.addClass("block block-page-title") }}>
    <div class="title-bar">
      {{ content }}
    </div>
  </header>
{% endif %}
