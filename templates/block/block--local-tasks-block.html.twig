{% extends "block.html.twig" %}
{#
/**
 * @file
 * Theme override for local tasks.
 */
#}
{% block content %}
  {% if content %}
    <nav class="local-tasks" role="navigation" aria-label="{{ 'Tabs'|t }}">
      {{ content }}
    </nav>
  {% endif %}
{% endblock %}
