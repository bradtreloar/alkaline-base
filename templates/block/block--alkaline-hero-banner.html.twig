{{ attach_library('alkaline/banner') }}
<header class="block">
  {{ title_prefix }}
  {{ title_suffix }}
  <figure class="banner hero full-page">
    <figcaption>
      {% block content %}
        {{ content|without('field_hero_background_image') }}
      {% endblock %}
    </figcaption>
  </figure>
</header>
{{ css }}
